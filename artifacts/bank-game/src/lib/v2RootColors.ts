/**
 * Centralized Economy v2 root section palette.
 * Import these — do not hardcode empty/ready colors in components or CSS.
 */

/** Empty / unfilled — warm light wood (not plastic-white). */
export const V2_ROOT_EMPTY_COLOR = "#c9a878";

/** Ready / collectible — saturated wood brown. */
export const V2_ROOT_READY_COLOR = "#7a4e2c";

/** Generating progress fill along the section (darker than empty, not fully ready). */
export const V2_ROOT_GENERATING_FILL_COLOR = "#9a6b3e";
